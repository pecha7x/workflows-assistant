<main class="container">
  <%= link_to sanitize("&larr; Back to Job Feed"), edit_polymorphic_path(@owner) %>

  <div class="header">
    <h1>Edit Notifier</h1>
  </div>

  <%= turbo_frame_tag @notifier do %>
    <%= render "form", owner: @owner, notifier: @notifier %>
  <% end %>  
</main>
