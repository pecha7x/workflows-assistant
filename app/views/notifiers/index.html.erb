<% if @notifiers.blank? %>
  <div class="empty-state">
    <p class="empty-state__text">
      You don't have any notifiers yet!
    </p>
  </div>
<% else %>
  <%= turbo_frame_tag "notifiers" do %>
    <%= render @notifiers %>
  <% end %>
<% end %>