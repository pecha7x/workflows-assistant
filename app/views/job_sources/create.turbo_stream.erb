<%= turbo_stream.update "new_job_source_button" do %>
  <%= link_to "New Job Source",
              new_job_source_path,
              class: "btn btn--primary",
              data: { turbo_frame: dom_id(JobSource.new) } %>
<% end %>
<%= turbo_stream.prepend "job_sources", @job_source %>
<%= turbo_stream.update JobSource.new, "" %>
<%= render_turbo_stream_flash_messages %>
