<main class="container">
  <%= link_to sanitize("&larr; Back to Job Feed"), job_feed_path(@job_feed) %>

  <div class="header">
    <h1>New Job Lead</h1>
  </div>

  <%= turbo_frame_tag @job_lead do %>
    <%= render "form", job_feed: @job_feed, job_lead: @job_lead %>
  <% end %>
</main>