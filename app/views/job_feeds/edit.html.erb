<main class="container">
  <%= link_to sanitize("&larr; Back to job feeds"), job_feeds_path %>

  <div class="header">
    <h1>Edit Job Feed</h1>
  </div>

  <%= turbo_frame_tag @job_feed do %>
    <%= render "form", job_feed: @job_feed %>
  <% end %>  
</main>
